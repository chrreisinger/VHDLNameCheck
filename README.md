VHDLNameCheck, an open source VHDL naming conventions checker.

To compile the project start the sbt(http://code.google.com/p/simple-build-tool/) console (use sbt.sh or sbt.bat) and run the compile command.